# Unlimited Pipe Gates

Unlimited Pipe Gates modifies the pipe gate to increase its throughput from one fluid launcher to 24. Currently the building HUD shows the correct L/min, however still shows one fluid launcher as that value is hard-coded.
